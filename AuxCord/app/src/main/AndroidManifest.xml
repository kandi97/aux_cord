<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pruales.auxcord">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".home_activity"> -->


        <!-- </activity> -->
        <activity android:name=".home" android:label="@string/home" />
        <activity android:name=".party_playlist"  android:label="@string/party_playlist" />
        <activity android:name=".my_picks" android:label="@string/my_picks"/>
        <activity android:name=".persona" android:label="@string/persona"/>
        <activity android:name=".create_party" android:label="@string/start_party"/>

        <!--</activity>-->


    </application>

</manifest>